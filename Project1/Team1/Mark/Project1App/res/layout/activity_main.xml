<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <EditText
        android:id="@+id/portText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/ipText"
        android:layout_toRightOf="@+id/ipText"
        android:ems="10"
        android:hint="Port" />

    <Button
        android:id="@+id/sendText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/getReading"
        android:layout_below="@+id/portText"
        android:onClick="sendDataPress"
        android:text="Send to\ndatabase" />

    <Button
        android:id="@+id/getReading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="getReading"
        android:text="Connect" />

    <EditText
        android:id="@+id/ipText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/getReading"
        android:layout_marginTop="18dp"
        android:layout_toLeftOf="@+id/startStop"
        android:ems="10"
        android:hint="IP Address" >

        <requestFocus />
    </EditText>

    <Button
        android:id="@+id/startStop"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/ipText"
        android:layout_toLeftOf="@+id/getReading"
        android:onClick="toggle"
        android:text="Start\nGPS" />

    <ListView
        android:id="@+id/dataText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/sendText"
        android:layout_below="@+id/sendText" >
    </ListView>

</RelativeLayout>